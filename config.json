{
  "paths": {
    "dir_data": "data/",
    "dir_output": "output/",
    "dir_train_images": "data/train/images",
    "dir_train_masks": "data/train/masks",
    "dir_test_images": "data/test/images",
    "train_image": "data/train/images/{}.png",
    "test_image": "data/test/images/{}.png",
    "train_mask": "data/train/masks/{}.png",
    "df_depths": "data/depths.csv",
    "df_train": "data/train.csv",
    "saved_train": "output/train.pk",
    "saved_test": "output/test.pk",
    "saved_model": "output/{}.model",
    "logfile": "output/log-{}",
    "submission": "output/submission_less sub {}.csv"
  },
  "final_predictions": false,
  "models_to_run": [
    "CustomResNet"
  ],
  "model_parameters": {
    "CustomResNet": {
      "use_saved_model": false,
      "optimal_cutoff": 0.5,
      "k_folds": 5,
      "kernel_initializer": "glorot_normal",
      "neurons_initial": 16,
      "batchnorm_momentum": 0.99,
      "optimizer_stage1": "adam",
      "lr_stage1": 0.01,
      "optimizer_momentum_stage1": 0.0,
      "optimizer_stage2": "adam",
      "lr_stage2": 0.01,
      "optimizer_momentum_stage2": 0.0,
      "early_stopping": 20,
      "batch_size_stage1": 32,
      "batch_size_stage2": 32,
      "epochs_stage1": 50,
      "epochs_stage2": 50,
      "dropout_expansion": true,
      "dropout_ratios_expansion": [0.25, 0.50, 0.50, 0.50],
      "dropout_contraction": true,
      "dropout_ratios_contraction": [0.50, 0.50, 0.50, 0.50]
    }
  }
}
